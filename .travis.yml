sudo: required
services:
  - docker

before_install:
  - docker build -t tonydecarlo11/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run tonydecarlo11/react-test npm test -- --coverage

after_success:
  - docker build -t tonydecarlo11/multi-client ./client
  - docker build -t tonydecarlo11/multi-nginx ./nginx
  - docker build -t tonydecarlo11/multi-server ./server
  - docker build -t tonydecarlo11/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push tonydecarlo11/multi-client
  - docker push tonydecarlo11/multi-nginx
  - docker push tonydecarlo11/multi-server
  - docker push tonydecarlo11/multi-worker
